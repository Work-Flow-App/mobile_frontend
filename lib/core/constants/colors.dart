import 'package:flutter/material.dart';

class AppColors {
  // Core colors
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const grey = Color(0xFF9E9E9E);

  // Success / Error
  static const green = Color(0xFF2E7D32); // Dark green
  static const red = Color(0xFFD32F2F); // Material red 700

  // Optional shades if you want lighter for light mode
  static const greenLight = Color(0xFF66BB6A);
  static const redLight = Color(0xFFEF5350);
}
